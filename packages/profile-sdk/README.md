# profile-sdk

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build profile-sdk` to build the library.

## Running unit tests

Run `nx test profile-sdk` to execute the unit tests via [Jest](https://jestjs.io).
